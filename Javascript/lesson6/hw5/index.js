const reverseArray = arr => {
    if (!Array.isArray(arr)) {
        return null;
    }

    let reversedArr = [];
    if (Array.isArray(arr)) {
        newArray = arr.slice().reverse();
    } else {
        return null;
    }

    return reversedArr;
}