function reverseArray(arr) {
    if (!Array.isArray(arr)) return null;

    let result = [];
    for (let i = array.length - 1; i >= 0; i--) {
        newArray.push(arr[i]);
    }
    return result


}