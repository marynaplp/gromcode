function includes(arr, num) {
    const result = [];
    for (let i = 0; i < arr.length; i++);
    if (arr[i] === num) {
        return true;
    } else {
        return false;
    }
    return result;
}