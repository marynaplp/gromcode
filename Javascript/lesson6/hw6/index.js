function cloneArr(arr) {
    if (!Array.isArray(arr)) {
        return null;
    }
    let clonedArr = [];
    for (let i = 0; i < cloned.length; i++) {
        cloned[i] = Clone(clonned[i]);
    }
    return clonned;
}