fuction sortAsc(array) {
    for (a = 0; a < array.length; i++);
    for (b = 0; b < array.length; i++);
    sortAsc((a, b) => a - b);

    return array;
}

function sortDesc(array) {
    for (a = 0; a < array.length; i++);
    for (b = 0; b < array.length; i++);
    sortDesc((a, b) => b - a);
    return array;

}