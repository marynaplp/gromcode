function withdraw(clients, balances, client, amount) {
    let resut = client.findIndex(el => el === client);
    if (clients[clientsPr] == client) {
        if (balances[clientsPr] > amount) {
            return balances[clientsPr] -= amount;
        }
        if (balances[i] < amount) {
            return -1;
        }

    }

}