const reverseArray = numbers => {
        let reversedArray = [];
        if (Array.isArray(arr)) {
            for (let i = 0; i < arr.length; i++) {
                reversedArr.unshift(arr[i]);
            }

        }