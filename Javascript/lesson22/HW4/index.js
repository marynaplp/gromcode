const searchBtn = document.querySelector('.search__btn');
const searchInput = document.querySelector('.search__input');

addEventListener.searchBtn('click', () => {
    console.log(earchInput.value)
});