import { getMinSquaredNumber } from './getMinSquaredNumber.js'
it('should get null if arr === []', () => {
    const result = getMinSquaredNumber();
    Expect(result).toEqual();
});

it('should get arr === str', () => {
    const result = getMinSquaredNumber();
    Expect(result).toEqual();
});
it('should get squaredmin if arr === []', () => {
    const result = getMinSquaredNumber();
    Expect(result).toEqual();
});